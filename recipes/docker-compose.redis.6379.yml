name: redis

services:
  redis:
    image: redis:alpine
    container_name: redis
    volumes:
      - redis:/data
    labels:
      - "lazy-tcp-proxy.enabled=true"
      - "lazy-tcp-proxy.ports=6379:6379"

volumes:
  redis:
